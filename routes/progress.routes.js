import { Router } from 'express';


const progressRouter = Router();

// progressRouter.post('/sign-up', signUp);
// progressRouter.post('/sign-in', signIn);
// progressRouter.post('/sign-out', signOut);
// progressRouter.post('/sign-out-all-devices', signOutAllDevices);

export default progressRouter;